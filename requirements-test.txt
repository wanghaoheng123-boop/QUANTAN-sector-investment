# Test-only dependencies for the Python suite (`tests/`, `quant_framework/`).
#
# Q110-T7b (2026-09-06) — this file exists because `requirements.txt` CANNOT BE
# INSTALLED. Line 13 declares `tradingagents>=0.2.0`, which does not exist on
# PyPI under that name; a clean `pip install -r requirements.txt` dies there:
#
#     ERROR: Could not find a version that satisfies the requirement
#     tradingagents>=0.2.0 (from versions: none)
#
# That was invisible for as long as nobody provisioned the environment from
# scratch — every local run inherits packages installed some other way. The
# pytest CI job added by Q110-T7 was the first clean install this project has
# ever done, and it failed on its first run. That is the job working.
#
# The application manifest is NOT fixed by this file — see findings-ledger
# Q110-T7b. This lists only what the SUITE imports, verified by walking the
# imports of tests/, quant_framework/, alpha_miner.py, multi_agent_factor_mining/
# and the trading-agents runtime shims: numpy, pandas, pydantic, fastapi and
# pytest, plus first-party modules. `tests/test_trading_agents_runtime.py` says
# in its own docstring that it runs "without importing the heavy tradingagents
# package", and `test_options_analyzers.py` skips itself when matplotlib is
# absent — so neither is a required dependency here.
#
# Keep the floors identical to requirements.txt so the suite never runs against
# an older library than the app declares.
numpy>=1.26.0
pandas>=2.1.0
# scipy is imported LAZILY inside alpha_miner.py:79, so an import-walk of the
# module's top-level statements does not see it. Found by running the suite in
# a clean venv, which is the only check that would have. Static import analysis
# is not a substitute for a cold install.
scipy>=1.12.0
pydantic>=2.0.0
fastapi>=0.115.0
pytest>=8.0.0
