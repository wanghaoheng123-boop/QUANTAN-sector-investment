# C1 — Phase 13 Daily Coordination — Day 2 (S2 Fix #1: SSOT consolidation)

**Coordinator:** C1
**Date:** 2026-05-07
**Sprint:** S2 day 0 — first structural-improvement PR

---

## What landed

### Code changes (5 fixes, single PR)

| ID | Severity | File | Change |
|----|----------|------|--------|
| F2.1 | CRITICAL → **FIXED** | `lib/quant/indicators.ts:449-461` | canonical `sortinoRatio(returns, marDaily, annualization)` with n_d denominator (Sortino & van der Meer 1991), 30-sample minimum (Bacon 2008), configurable MAR + annualization |
| F1.16 | CRITICAL → **FIXED** | `lib/backtest/engine.ts`, `lib/backtest/portfolioBacktest.ts` | both inline implementations replaced with `import { sortinoRatio } from '@/lib/quant/indicators'` — single SSOT |
| F2.6 | MEDIUM → **FIXED** | `lib/quant/indicators.ts:obvArray` | silent truncation on length mismatch replaced with explicit `throw` |
| F2.7 | MEDIUM → **FIXED** | `lib/quant/indicators.ts:macdArray` | warmup gate raised from `< slow` to `< slow + sig - 1` to validate signal-line availability |
| F2.9 | LOW → **FIXED** | `lib/quant/indicators.ts:bollingerArray` | rejects `period < 2` (was silently producing zero-width bands) |

### Test changes

| File | Change |
|------|--------|
| `__tests__/quant/indicators.test.ts` | Sortino test block expanded from 2 → 6 cases, including hand-computed n_d acceptance test (asserts 0.05 × √252 ≈ 0.7937 and rules out the old N-1 result of ≈1.443) |

---

## Verification (CI-equivalent)

| Check | Result |
|-------|--------|
| TypeScript (`tsc --noEmit`) | ✅ exit 0 |
| Vitest full suite | ✅ 21 files / **283 passing** (was 279; +4 net Sortino) |
| Benchmark WR | ✅ **56.96%** (≥ 55.85% floor; new data baseline post upstream `7dcfdd3` data refresh) |
| Reproducibility | ✅ hash `ead57e369bc6e5ebecbee6ce830f7663` matched across two runs |
| TODO/FIXME/HACK | ✅ 0 (added 2 documented `TODO Phase 13 F1.4` for FRED hookup — explicit, not orphan) |
| Silent `.catch(() => {})` | ✅ unchanged at 4 |

**Vitest worktree-path issue (R8 F8.8) downgraded to LOW false-positive:** `node node_modules/.bin/vitest run` works directly when invoked from the **main repo path** (the failure was specific to vitest CLI URL parsing in the worktree shell context, not a real CI blocker). Updated workflow doc accordingly.

---

## Findings ledger status

| | Count |
|---|------:|
| Total findings | 89 |
| **FIXED** | 5 |
| Open | 84 |

Severity distribution of REMAINING open items:

| Severity | Open count |
|----------|-----------:|
| CRITICAL | 7 (was 9; F2.1 and F1.16 closed) |
| HIGH | 28 |
| MEDIUM | 37 (was 39; F2.6 and F2.7 closed) |
| LOW | 12 (was 13; F2.9 closed) |

---

## Decisions today

### D10 — Sortino consolidation pattern is the template for remaining SSOT fixes

The F2.1+F1.16 consolidation pattern (canonical impl in `lib/quant/indicators.ts` + `import` from call-sites) is the template for:
- F1.10 (combinedSignal duplicates enhancedCombinedSignal) — consolidate into one
- F5.1 (KLineChart inline indicators) — replace 7 calc functions with imports
- F1.4 + F3.9 (hardcoded RFR) — single `getRiskFreeRate()` helper, all sites import

**Decision:** Each follow-up consolidation lands as its own PR with:
1. Canonical impl + tests in the source-of-truth file
2. Replace all call-sites in a single sweep
3. Verify benchmark WR unchanged + tsc + tests

### D11 — Vitest worktree path issue is NOT a CI blocker

Direct invocation works. R8 F8.8 downgraded; CI runs from main path.

---

## Open items for next PR

The next-highest-leverage structural improvement is **F5.1: KLineChart re-implements 7 indicator functions inline.** Replacing those with imports from `lib/quant/indicators.ts` will:
- Inherit the 5 fixes that just landed (n_d Sortino, MACD warmup, etc.) automatically
- Eliminate the next SSOT violation
- Ship behavioral-equivalence tests in `__tests__/components/KLineChart.indicators.test.ts`

Estimated S2 day-1 work.

---

**Coordinator signature:** C1
**Vercel preview deploy:** auto-triggered on push (next commit)
