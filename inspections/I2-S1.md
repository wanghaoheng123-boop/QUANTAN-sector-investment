# I2 — Code Quality Inspector — S1 Audit

**Inspector:** I2 — independent code-quality enforcer
**Sprint:** S1
**Date:** 2026-05-05
**Mandate:** enforce file-size cap (≤500 LOC components, ≤600 libs), zero new `any`, zero new `.catch(()=>{})`, zero new circular imports, no duplicate logic.

---

## Baseline metrics (frozen for S2 entry)

### File-size violations (current state)

| File | LOC | Cap | Excess |
|------|-----|-----|--------|
| `components/stock/QuantLabPanel.tsx` | 1649 | 500 | **+1149** |
| `components/KLineChart.tsx` | 1011 | 500 | **+511** |
| `app/backtest/page.tsx` | 934 | 500 | **+434** |
| `app/crypto/btc/page.tsx` | 814 | 500 | **+314** |
| `lib/backtest/signals.ts` | 691 | 600 | **+91** |
| `lib/backtest/engine.ts` | 677 | 600 | **+77** |
| `app/ma-deviation/page.tsx` | 616 | 500 | **+116** |
| `app/page.tsx` | 552 | 500 | **+52** |
| `app/stock/[ticker]/page.tsx` | 539 | 500 | **+39** |
| `components/crypto/BtcQuantLab.tsx` | 516 | 500 | **+16** |

**Floor:** No PR may introduce a new file exceeding the cap. PRs reducing the LOC of files above are encouraged (S3 sprint).

### Silent `.catch(() => {})` count

```
4 instances confirmed (matches Phase 12 baseline):
  components/NewsFeed.tsx:63
  app/sector/[slug]/page.tsx:76
  app/sector/[slug]/page.tsx:128
  components/stock/QuantLabPanel.tsx:450
```
**Floor:** No new instances permitted. S2 backlog: reduce to 0.

### `Math.random` in `lib/`

```
0 instances. Floor: 0.
```

### TODO/FIXME/HACK in `lib/` + `app/` + `components/`

```
0 instances. Floor: 0.
```

### Inline-style usages (not a hard cap; tracked for S3)

```
37 instances across components/. Target: ≤ 10 (S3).
```

### ARIA-attribute coverage (cross-reference to R6)

```
32 occurrences of aria-label / aria-describedby / aria-live / role= 
across components/ + app/. Target: ≥ 100 by S4 end.
```

### TypeScript any usage (sampled)

```
$ grep -rn ": any\b" lib app components hooks --include="*.ts" --include="*.tsx" | wc -l
~12 occurrences (sampled — not full audit).
```
**Floor:** No new `any` permitted. Track via `npx type-coverage --at-least 98` once added.

---

## Spot-checks on R5

### Spot-check #1 — R5 F5.1 (KLineChart re-implements indicators)

**Independent verification:**
- `grep "^function calc" components/KLineChart.tsx` returns 7 functions: calcEMA, calcRSI, calcMACD, calcBollingerBands, calcVWAP, calcATR, calcVolumeSMA.
- Each duplicates a function in `lib/quant/indicators.ts`.
- This violates Phase 13 plan rule "Single canonical EMA, RSI, formatter."

**Verdict:** SUSTAINED. Critical severity confirmed.

### Spot-check #2 — R5 F5.4 (silent catches)

Re-grepped:
```
$ grep -rn "\.catch(\s*(\s*)\s*=>\s*{\s*}\s*)" components app hooks --include="*.tsx" --include="*.ts"
4 hits: NewsFeed.tsx:63, sector:76, sector:128, QuantLabPanel:450
```
**Verdict:** SUSTAINED. Matches Phase 12 baseline of 4.

### Spot-check #3 — R5 F5.10 (local formatters duplicating lib/format.ts)

- `app/backtest/page.tsx:39-50` defines `fmtPct`, `fmtMoney`, `fmtRatio`, `MetricCard`.
- `lib/format.ts` exports `formatCompactNumber`, `formatCurrency`, `formatSignedNumber`.
- Functional overlap confirmed.

**Verdict:** SUSTAINED.

---

## Duplicate-logic detection (manual sample)

Without jscpd installed, manual sampling of duplicate-pattern hot spots:

| Pattern | Locations | Severity |
|---------|-----------|----------|
| Indicator calc functions | KLineChart (7 fns), `lib/quant/indicators.ts`, `lib/quant/btc-indicators.ts` | HIGH |
| Format helpers | `app/backtest/page.tsx`, `app/crypto/btc/page.tsx`, etc. (estimated 3-5 reimplementations) | MEDIUM |
| Sortino impl | `lib/quant/indicators.ts:449-461` AND `lib/backtest/engine.ts:387-396, 530-540` | CRITICAL (math divergence) |
| Card container className | "rounded-xl border border-slate-800 bg-slate-950" with variations | MEDIUM |

S3 entry condition: `npx jscpd lib app components --threshold 3` reports < 3% duplication.

---

## Standing-prompt rule audit (rule 9: "no template approve")

I sampled R1, R3, R5 reports for "template approve" patterns (e.g. boilerplate "looks good"):
- R1: 15 distinct findings, each with file:line + acceptance test → no template approve.
- R3: 8 findings, each cites Hull/Merton/Wilder by page or canonical reference → no template approve.
- R5: every finding has at least one substantive observation → no template approve.

**Verdict:** all reviewer reports comply with rule 9.

---

## Verdict

S1 audit findings on code quality SUSTAINED. The four "god component" file-size violations (QuantLabPanel, KLineChart, app/backtest/page, app/crypto/btc/page) are the dominant S3 work-package. The two-Sortino divergence (R2 F2.1 + I1 spot-check #2) is the dominant S2 SSOT-correctness item.

**S1 → S2 gate (code quality):** APPROVED with two conditions:
1. R5 second-pass on remaining god components before S3 entry.
2. Add jscpd + type-coverage to CI in S2 (E4 deliverable).

---

**Inspector signature:** I2 (code quality)
**Veto power:** not exercised
