# I4 — End-to-End Inspector — S1 Audit

**Inspector:** I4 — independent E2E + reproducibility verifier
**Sprint:** S1
**Date:** 2026-05-05
**Mandate:** run `check:smoke:extended`, backtest reproducibility (hash-match across two runs), Vercel preview parity, capture evidence (logs, screenshots, hashes).

---

## Reproducibility — VERIFIED ✅

Captured at S1 day 0:

```bash
$ node scripts/benchmark-signals.mjs > /tmp/qan_run1 2>&1
$ node scripts/benchmark-signals.mjs > /tmp/qan_run2 2>&1
$ diff /tmp/qan_run1 /tmp/qan_run2
$ echo $?
0
$ md5 /tmp/qan_run1 /tmp/qan_run2
MD5 (/tmp/qan_run1) = e0897f03c2c4bc1934df4a65700c8834
MD5 (/tmp/qan_run2) = e0897f03c2c4bc1934df4a65700c8834
$ md5 scripts/benchmark-results.json
MD5 (scripts/benchmark-results.json) = 72f1fd22fcbeb55201096e2834f0bf7f
```

**Result:** Both runs produce byte-identical output. Hash `e0897f03c2c4bc1934df4a65700c8834` is the S1 reproducibility seal. Any S2/S3/S4 PR that changes this hash without explicit C2 sign-off triggers I4 veto.

---

## Algorithm performance baseline (frozen)

```
Instruments:                 56
Instruments with trades:     55
Total BUY signals:         1393
Wins:                       785
Losses:                     608
Aggregate Win Rate:       56.35%
Avg Win Rate per Instrument: 58.97%
Avg 20d Return per Signal: 1.2541%
```

**Floor:** any S2 PR that drops Aggregate Win Rate below 55.85% is auto-blocked at CI.

---

## Test suite — DEFERRED ⚠️

`vitest run` blocked locally by space-in-path issue from worktree shell context (vite-internal `_setServer` URL parsing — see R8 F8.8). Test count captured by static grep:

```
Total test cases: 279
Test files:        21
```

**Action item for I4 day 1:** run `npm run test` from the main worktree path (without the spaces-in-path `/.claude/worktrees/...` prefix) and capture `npm run test:coverage` output.

---

## Smoke tests — DEFERRED to I4 day 1

`npm run check:smoke:extended` not yet run in this S1 day-0 pass. Captured for day 1:
- `npm run check:smoke:extended` against local `npm run dev`
- `npm run check:smoke` against the production Vercel deployment

**Floor (TBD on day 1):** all smoke checks green.

---

## Vercel preview parity — DEFERRED to I4 day 1

To verify on day 1:
- Production deploy hash matches main branch HEAD
- `https://quantan.vercel.app/api/prices?tickers=SPY` returns expected schema
- `https://quantan.vercel.app/api/chart/spy?range=1Y` returns expected schema
- `https://quantan.vercel.app/api/trading-agents/health?deep=1` returns `{status: 'healthy'}` IF Railway sidecar deployed (per AGENTS.md Phase 4)

---

## Code-quality CI snapshot — captured

```
TODO/FIXME/HACK count:        0
Silent .catch(() => {}) count: 4
Math.random in lib/:          0
inline style usages:          37
ARIA attribute count:         32
modules without tests:        11 (per R8 F8.1)
```

These are the S1 baseline. Any drift in S2/S3/S4 is automatically flagged.

---

## Outstanding spot-checks before S2 entry

I4 will block S2 entry until the following are captured:
1. Coverage report from `npm run test:coverage` (not running locally; may need fixed worktree path or remote CI run)
2. `npm run check:smoke:extended` green
3. Vercel production parity confirmed

These are scheduled for I4 day 1.

---

## Cross-inspector coordination

| Item | Owner | Sprint |
|------|-------|--------|
| Reproducibility hash | I4 (this report) | S1 ✅ |
| Math citation audit | I1 | S1 ✅ |
| Code-quality file-size audit | I2 | S1 ✅ |
| Compliance scope audit | I3 | S1 ✅ |
| Coverage report | I4 | S1 day 1 ⚠️ |
| Smoke + production parity | I4 | S1 day 1 ⚠️ |

---

## Verdict

**S1 → S2 gate (E2E + reproducibility):** CONDITIONAL APPROVAL pending I4 day 1 captures of:
- `npm run test:coverage`
- `npm run check:smoke:extended`
- Vercel production parity

Reproducibility is GREEN. Algorithm baseline is FROZEN. CI artifacts captured.

---

**Inspector signature:** I4 (E2E + reproducibility)
**Veto power:** held; will exercise if day 1 captures fail
