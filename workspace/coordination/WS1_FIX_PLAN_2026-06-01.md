# WS1 Security Draft — Diagnosis & Exact Fix Plan — 2026-06-01

Coordinator diagnosis of the broken WS1 draft (`fix/ws1-api-security` @ `32506bd`), captured durably
so it survives a harness stall.

## ✅ RESOLVED 2026-06-01 — branch `fix/ws1-api-security-fixed` @ `58b49c3`
The fix is applied, committed, and verified (details below). This branch supersedes the broken
`fix/ws1-api-security` draft. 6 files: `lib/auth/apiKey.ts` (helper, kept) + route auth wiring
(`trading-agents`) + rate limits (`analytics`, `fundamentals`) + 2 rewritten real tests.
- **tsc --noEmit:** clean (0 errors) — authoritative `@/` resolution check.
- **vitest:** full suite **992 pass / 17 skip / 0 fail (84 files)**, +13 new vs 979 baseline. (Tests
  use `vi.hoisted` mocks; an earlier commit shipped a TS2556 spread error + a `chartMock` TDZ hoist
  error — both fixed in `58b49c3`.)
- **Local caveat (proven, not a defect):** `vite-tsconfig-paths` + symlinked `node_modules` can't
  resolve worktree-only NEW files (`@/lib/auth/apiKey`), so the 2 alias tests only run under real
  `node_modules`/CI. Proven locally via relative-import smoke (helper 4/4) + temp-root-resolution
  e2e (13/13). Root left pristine after each temp check.
- **Bucket B:** NOT pushed/PR'd. OWNER decides auth posture (provision `QUANTAN_API_KEY` vs
  require-session-only).

The diagnosis below is retained for the record.

## What WS1 actually committed (3 files)
- `lib/auth/apiKey.ts` — ✅ **GOOD, KEEP.** `isValidApiKey()` is fail-closed (rejects when
  `QUANTAN_API_KEY` unset) and constant-time over fixed-width SHA-256 digests. Correct.
- `__tests__/api/trading-agents-auth.test.ts` — ⚠️ MIXED. Block 1 (`describe('isValidApiKey…')`,
  lines 35–67) is VALID and tests the helper directly — KEEP. Block 2 (`describe('POST …auth gate')`,
  lines 69–142) is FICTION — mocks `@/lib/agents/orchestrator` (`runTradingAgents`) and
  `@/lib/auth/session` (`getServerSession`), **neither exists**. The real route imports
  `getServerSession` from `next-auth` and `getAuthOptions` from `@/lib/auth`, and proxies to a Python
  sidecar via `fetch` (no `runTradingAgents`). DELETE block 2.
- `__tests__/api/route-rate-limit.test.ts` — ❌ FICTION. Mocks `@/lib/providers/yahoo` (doesn't
  exist; real routes do `new YahooFinance()` from `yahoo-finance2` directly, so the mock intercepts
  nothing → real network calls). DELETE / rewrite.

## Correction (verified 2026-06-01): NO existing route tests exist
An earlier draft of this plan claimed `trading-agents-route.test.ts` / `-extended` /
`analytics-route.test.ts` / `fundamentals-route.test.ts` exist and bake in the bypass. **That was
wrong** — `find __tests__ -name '*.test.ts'` shows NONE of them exist. `__tests__/api/` holds only:
csrf, marketHours, prices, rateLimit(.kv), reliability, sanitize(.fuzz), sector-rotation. So there is
**nothing to retrofit** — the fix is purely: secure the route + add NEW correct tests. (This is the
same fabrication-class error the WS1 agent made; caught by listing the dir before trusting memory.)

## House test pattern to follow (from `__tests__/api/sector-rotation.test.ts`)
`vi.mock('yahoo-finance2', () => ({ default: class { chart = chartMock } }))` +
`vi.mock('@/lib/api/rateLimit', () => ({ applyRateLimit: vi.fn(() => null) }))`, then
`import { GET/POST } from '@/app/api/.../route'` and drive with `NextRequest`. For the trading-agents
POST, also `vi.mock('next-auth', () => ({ getServerSession: vi.fn() }))` and
`vi.mock('@/lib/api/csrf', ...)` (or send matching cookie+header). `getAuthOptions` is real at
`lib/auth.ts:76` — no need to mock `@/lib/auth`.

## Exact D4-1 route fix (`app/api/trading-agents/[ticker]/route.ts`)
Add import: `import { isValidApiKey } from '@/lib/auth/apiKey'`
- Line ~164–165: keep `const apiKeyHeader = req.headers.get('x-api-key')`, but change the CSRF bypass
  from `if (!apiKeyHeader && !validateCsrf(req))` → `if (!isValidApiKey(apiKeyHeader) && !validateCsrf(req))`.
- Line ~178: change auth check from `if (!session?.user && !apiKeyHeader)` →
  `if (!session?.user && !isValidApiKey(apiKeyHeader))`.
Net: only a VALID key (constant-time match vs configured `QUANTAN_API_KEY`) bypasses CSRF / satisfies
auth. Bare/invalid header no longer works. Session path unchanged.

## D4-3 rate-limit fix (analytics + fundamentals)
Both routes import nothing from rateLimit yet. Pattern (from `lib/api/rateLimit.ts`):
`import { applyRateLimit } from '@/lib/api/rateLimit'`, then at the top of GET:
`const rl = await applyRateLimit(req, 'analytics'|'fundamentals', { maxRequests: 30, windowSeconds: 60 }); if (rl) return rl`
- `analytics/[ticker]/route.ts`: GET signature is `(_req: Request, …)` → rename `_req`→`req`.
- `fundamentals/[ticker]/route.ts`: already `(req: NextRequest, …)`.
- RISK: check `analytics-route.test.ts` / `fundamentals-route.test.ts` don't loop >30 same-IP calls
  (would trip 429). If they do, the limiter must be KV-disabled + per-test IP, or the tests adjusted.

## Tests to write (house pattern — mock next-auth + @/lib/auth + trading-agents-config + global.fetch)
1. Update `trading-agents-route.test.ts` + `trading-agents-extended.test.ts`: set
   `process.env.QUANTAN_API_KEY` and use the matching key (or mock an authenticated session) so the
   200-expecting cases stay valid under fail-closed; add a case asserting a bogus key → 401.
2. Replace agent's `trading-agents-auth.test.ts` block 2 with a correct route auth test (or fold into
   the existing file and delete the agent's file). Keep block 1 (helper unit tests).
3. Rewrite `route-rate-limit.test.ts` with the real yahoo mock (`vi.mock('yahoo-finance2', …)`) +
   per-test IP, asserting 30→200 then 429.

## Mechanics
- Lock owner of the WS1 worktree (pid 40735) was ALIVE → do NOT write into
  `.claude/worktrees/agent-a34329d305d3f85bc`. Branch a fresh worktree off `fix/ws1-api-security`
  (e.g. `fix/ws1-api-security-fixed`), keep the helper commit, apply the above, supersede the draft.
- Gates: symlink node_modules, `node node_modules/typescript/bin/tsc --noEmit` +
  `node node_modules/vitest/dist/cli.js run`. Baseline 979 pass / 17 skip must hold (plus the updated
  trading-agents/rate-limit cases).
- Bucket B → still NO push/PR/merge. OWNER decides: provision `QUANTAN_API_KEY` (re-enables the
  X-API-Key path) vs require-session-only (fail-closed leaves API-key path disabled until a key is set).
